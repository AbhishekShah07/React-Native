import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  mainView: {
    alignSelf: 'center',
    marginTop: 10,
    marginLeft: 20,
    marginRight: 20,
    width: '90%',
    alignItems: 'center',
  },
  buttonView: {
    width: '100%',
  },
});

export default styles;
